Настройка Git и доступ к удалённому репозиторию из текущей папки проекта.

Контекст: локальный репозиторий в C:\__docs_new уже содержит настроенный `origin` → https://github.com/alex-lysenko-de/avl.git. Репозиторий проверен, доступен, пока пуст (ни одного push).

Что сделать:
* убедиться, что `origin` настроен и доступен (`git remote -v`, `git ls-remote origin`);
* задокументировать это в wiki (раздел «Процесс разработки» / «Git»);
* выполнить первый push текущей истории в `origin/main` (подтверждено пользователем — пушить по ходу выполнения тикетов).

